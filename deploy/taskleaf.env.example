NODE_ENV=production
HOST=127.0.0.1
PORT=3000
DATA_FILE=/var/lib/taskleaf/tasks.json
BACKUP_DIR=/var/backups/taskleaf
# Replace PUBLIC_IP:8080 with the actual Nginx address. Keep loopback for health checks.
ALLOWED_HOSTS=127.0.0.1:3000,PUBLIC_IP:8080
